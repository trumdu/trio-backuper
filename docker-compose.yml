services:
  app:
    build: .
    container_name: trio-backuper
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/data
      # Jobs are managed via local config.json (keep it out of git)
      - ./config.json:/app/config.json:ro
    restart: unless-stopped
